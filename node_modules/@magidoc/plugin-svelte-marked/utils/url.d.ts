import type { Slugger } from 'marked';
export declare function joinUrlPaths(...paths: string[]): string;
export declare function isRelative(url: string): boolean;
export declare function generatePathSegment(name: string, slugger: Slugger): string;
