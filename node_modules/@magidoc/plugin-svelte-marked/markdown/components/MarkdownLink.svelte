<script>import { isRelative, joinUrlPaths } from '../../utils/url';
export let token;
export let options;
export const renderers = undefined;
</script>

<a
  href={isRelative(token.href)
    ? joinUrlPaths(options.baseUrl, token.href)
    : token.href}
  title={token.title}
>
  <slot />
</a>
