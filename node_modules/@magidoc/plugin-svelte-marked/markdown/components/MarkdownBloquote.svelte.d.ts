import { SvelteComponentTyped } from "svelte";
import type { marked } from 'marked';
import type { MarkdownOptions, Renderers } from '../markedConfiguration';
declare const __propDef: {
    props: {
        token?: marked.Tokens.Blockquote;
        options?: MarkdownOptions;
        renderers?: Renderers;
    };
    events: {
        [evt: string]: CustomEvent<any>;
    };
    slots: {
        default: {};
    };
};
export type MarkdownBloquoteProps = typeof __propDef.props;
export type MarkdownBloquoteEvents = typeof __propDef.events;
export type MarkdownBloquoteSlots = typeof __propDef.slots;
export default class MarkdownBloquote extends SvelteComponentTyped<MarkdownBloquoteProps, MarkdownBloquoteEvents, MarkdownBloquoteSlots> {
    get token(): marked.Tokens.Blockquote;
    get options(): MarkdownOptions;
    get renderers(): Renderers;
}
export {};
